name: Python Lint

on:
  push:
    branches: [main]
  pull_request: {}

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: chartboost/ruff-action@v1
        with:
          src: './src/'
      - uses: chartboost/ruff-action@v1
        with:
          src: './src/'
          args: 'format --check'
